pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/Premkumar734/jenkins-Docker-project.git'
            }
        }

        stage('Build Image') {
            steps {
                sh 'docker build -t mywebsite .'
            }
        }

        stage('Deploy') {
            steps {
                sh 'docker stop mywebsite || true'
                sh 'docker rm mywebsite || true'
                sh 'docker run -d -p 80:80 --name mywebsite mywebsite'
            }
        }
    }
}
~                                                                                                                                                                        
~                                                                                                                                                                        
~                                          
